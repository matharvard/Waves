<%= form_for @post, :url => posts_path, :html => { :multipart => true } do |f| %>
  <%= hidden_field_tag :post_type, @post.type %>
  <%= f.error_messages %>
  
  <%= form_fields_for @post, f %>
  
  <div class="actions">
    <%= f.submit %>
  </div> <!-- end.actions -->
<% end %>
